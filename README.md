## MapNik Server Usage

    sudo pip install -r requirements.txt
    python manage.py migrate
    python manage.py createsuperuser  # Follow steps
    python manage.py runserver <IP>:<PORT>


Data created at `add_location` goes to /admin/